(() => { const field = document.querySelector('.ambient-field'); const orbs = [...document.querySelectorAll('.ambient-orb')]; let target = 0, current = 0; addEventListener('scroll', () => { target = scrollY; }, {passive:true}); function animate(){ current += (target-current)*.055; field.style.transform = `translate3d(0,${-current*.028}px,0)`; orbs.forEach((orb,index) => orb.style.setProperty('--scroll-rotation', `${current*(index+1)*.004}deg`)); requestAnimationFrame(animate); } animate(); })();
